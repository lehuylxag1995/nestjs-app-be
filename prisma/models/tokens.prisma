model RefreshToken {
    id        String   @id @default(uuid())
    tokenHash String
    expiresAt DateTime
    device    String? // ví dụ: 'Chrome-Win10', 'iPhone'
    userId    String
    createdAt DateTime @default(now())

    user User @relation(fields: [userId], references: [id], onDelete: Cascade)

    @@unique([userId, device])
}
